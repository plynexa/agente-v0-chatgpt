# Optional providers. The V0 must run without these values.
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_MODEL=

# LAN is disabled by default. Generate, do not reuse, device tokens.
AGENT_LAN_ENABLED=false
AGENT_LAN_BIND_HOST=127.0.0.1
AGENT_LAN_PORT=8787
